#ifndef RECURSION_H
#define RECURSION_H

#include <vector>

int pow(int x, int n);
int binarySearch(const std::vector<int>& v, int valToSearchFor, int lo, int hi);

#endif /* end of include guard: RECURSION_H */
